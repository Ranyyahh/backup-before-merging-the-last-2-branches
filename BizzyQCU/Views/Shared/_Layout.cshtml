@{
    var pageClass = ViewBag.PageClass as string ?? string.Empty;
    var showBackButton = ViewBag.ShowBackButton != null && (bool)ViewBag.ShowBackButton;
    var hideGlobalHeader = ViewBag.HideGlobalHeader != null && (bool)ViewBag.HideGlobalHeader;
    var pageTitle = ViewData["Title"] ?? ViewBag.Title;
    var backUrl = ViewBag.BackUrl as string;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@pageTitle - BizzyQCU</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    @Styles.Render("~/Content/css")
    <link rel="stylesheet" href="@Url.Content("~/Content/layout.css")">
    <link rel="stylesheet" href="@Url.Content("~/Content/home_about_product_ent.css")">
    <link rel="stylesheet" href="@Url.Content("~/Content/MobileNavbar.css")">
    @if (IsSectionDefined("Styles"))
    {
        @RenderSection("Styles", required: false)
    }
    else if (IsSectionDefined("styles"))
    {
        @RenderSection("styles", required: false)
    }
</head>
<body class="app-body @pageClass">

    @if (!hideGlobalHeader)
    {
        <div class="navbar">

            @if (showBackButton)
            {
                <a href="javascript:history.back()" class="mobile-back-btn">
                    <img src="@Url.Content("~/Content/images/back-btn.png")" class="back-icon" />
                </a>
            }

            <div class="logo">
                <a href="/"><img src="@Url.Content("~/Content/images/Logo.png")" alt="logo"></a>
            </div>

            <div class="divider"></div>

            <div class="nav-links" id="mainNav">
                <a href="@Url.Action("Homepage", "Home")">HOME</a>
                <a href="@Url.Action("ProductList", "ProductList")">PRODUCTS</a>
                <a href="#">ENTERPRISES</a>
                <a href="@Url.Action("About", "Home")">ABOUT US</a>

                <a href="#" class="account-mobile">
                    ACCOUNT
                </a>

            </div>

            <div class="right-icons">
                <div class="cart-icon">
                    <a href="#"><img src="@Url.Content("~/Content/images/cart-icon.png")"></a>
                </div>
                <div class="account-icon account-desktop">
                    <img src="@Url.Content("~/Content/images/account-icon.png")">
                </div>


                <button class="hamburger" id="hamburgerBtn">
                    &#9776;
                </button>
            </div>

        </div>
    }

    <main class="page-root">
        @if (showBackButton)
        {
            <button class="back-button" type="button" onclick="@(string.IsNullOrWhiteSpace(backUrl) ? "if (window.history.length > 1) { window.history.back(); } else { window.location.href = '" + Url.Action("Index", "Home") + "'; }" : "window.location.href = '" + backUrl + "';")" aria-label="Go back">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                    <path d="M14.5 6.5 9 12l5.5 5.5"></path>
                </svg>
            </button>
        }

        @if (TempData["FlashMessage"] != null)
        {
            <div class="flash-message" role="status">@TempData["FlashMessage"]</div>
        }

        @RenderBody()
    </main>
    <br />
    <br />


    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    @if (IsSectionDefined("Scripts"))
    {
        @RenderSection("Scripts", required: false)
    }
    else if (IsSectionDefined("scripts"))
    {
        @RenderSection("scripts", required: false)
    }


    <!--FOR HAMBURGER TOGGLE-->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const btn = document.getElementById("hamburgerBtn");
            const nav = document.getElementById("mainNav");
            const body = document.body;

            if (!btn || !nav) {
                return;
            }

            btn.addEventListener("click", function () {
                nav.classList.toggle("open");
                body.classList.toggle("menu-open", nav.classList.contains("open"));
            });

            nav.querySelectorAll("a").forEach(function (link) {
                link.addEventListener("click", function () {
                    nav.classList.remove("open");
                    body.classList.remove("menu-open");
                });
            });
        });


    </script>
</body>
</html>
